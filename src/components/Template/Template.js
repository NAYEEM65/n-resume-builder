import React from 'react';
import Layout from '../../Layout/Layout';

const Template = () => {
    return (
        <Layout>
            <h1>Template</h1>
        </Layout>
    );
};

export default Template;
